# python
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/
.venv/

# raw data (hundreds of MB, downloaded via scripts) — NOT committed
data/raw/
*.h5ad
*.mtx
*.mtx.gz
*.tsv.gz
# the small preprocessed npz (~6 MB) + labels ARE committed so the repo
# clones runnable out-of-the-box (full reproduction via scripts/prep_*.py)
**/oof_folds/
**/*.pkl
data/results/

# keep result JSONs + logs + the figure (they are small + are the evidence)
!results/*.json
!results/*.log
!figure_comparison.png

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
